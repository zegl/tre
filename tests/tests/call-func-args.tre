func otherfunc(num i64) i64 {
    // in other func = 100
    printf("in other func = %d\n", num)
    return num + 10
}

func main() i64 {
    res := otherfunc(100)
    // in main func = 110
    printf("in main func = %d\n", res)
    return 0
}
