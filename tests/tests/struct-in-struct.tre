type Book struct {
    book_id i64
}

type Bookshelf struct {
    book0 Book
    book1 Book
}

func main() i64 {
    shelf := new Bookshelf
    shelf.book0 = new Book
    shelf.book0.book_id = 1000

    shelf.book1.book_id = 2000

    // 1000
    printf("%d\n", shelf.book0.book_id)

    // 2000
    printf("%d\n", shelf.book1.book_id)

    return 0
}
